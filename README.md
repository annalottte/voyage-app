# voyage-app

## AI configuration

Set these environment variables for the serverless AI endpoints:

- `ANTHROPIC_API_KEY` (required)
- `ANTHROPIC_MODEL` (optional, default model for AI day tips and fallback for summary)
- `ANTHROPIC_SUMMARY_MODEL` (optional, overrides summary model only)